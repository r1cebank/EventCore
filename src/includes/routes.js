/*
 *  This file defines the routes in the application, each route must have a
 *  valid component
 */

const routes = [
    {
        key: 'home',
        title: 'home',
        component: 'home'
    }
];

export default routes;
