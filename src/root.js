/* @flow */

import React from 'react-native';

import RootView from './navigation/router/router';

// Construct the root element
class Root extends React.Component {
    render() {
        return (
            <RootView />
        );
    }
}

export default Root;
